import React,{useState} from 'react';interface StoryEditorProps{personId:string;initialStory?:string;}export const StoryEditor:React.FC<StoryEditorProps>=({personId,initialStory=''})=>{
  console.log(personId);
const[story,setStory]=useState(initialStory);const[isEditing,setIsEditing]=useState(false);return<div className="story-editor"><div className="story-header"><h3>Biografia</h3><button onClick={()=>setIsEditing(!isEditing)}>{isEditing?'Salva':'Modifica'}</button></div>{isEditing?<textarea value={story}onChange={(e)=>setStory(e.target.value)}placeholder="Scrivi la storia della persona..."className="story-textarea"/>:<div className="story-display">{story||'Nessuna storia aggiunta ancora'}</div>}</div>;};export default StoryEditor;
