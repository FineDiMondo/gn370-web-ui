import React,{useState,useMemo} from 'react';interface SearchViewProps{allPeople?:any[];}export const SearchView:React.FC<SearchViewProps>=({allPeople=[]})=>{const[query,setQuery]=useState('');const[filters,setFilters]=useState({gender:'all',birthYear:'',deathYear:''});const results=useMemo(()=>allPeople.filter(p=>p.name.toLowerCase().includes(query.toLowerCase())),[query,allPeople]);return<div className="search-view"><h2>Ricerca Persone</h2><div className="search-filters"><input type="text"placeholder="Nome, luogo, occupazione..."value={query}onChange={(e)=>setQuery(e.target.value)}className="search-box"/><select value={filters.gender}onChange={(e)=>setFilters({...filters,gender:e.target.value})}><option value="all">Tutti</option><option value="M">Maschi</option><option value="F">Femmine</option></select></div><div className="search-results"><p>{results.length} risultati</p>{results.slice(0,20).map((person,idx)=>(<div key={idx}className="result-item"><h4>{person.name}</h4><p>{person.birthDate?.substring(0,4)} • {person.birthPlace}</p></div>))}</div></div>;};export default SearchView;
