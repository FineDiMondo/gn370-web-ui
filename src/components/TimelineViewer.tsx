import React,{useMemo} from 'react';interface TimelineEvent{date:string;title:string;description?:string;type:'birth'|'marriage'|'death'|'event';}interface TimelineViewerProps{personId:string;events?:TimelineEvent[];}export const TimelineViewer:React.FC<TimelineViewerProps>=({personId,events=[]})=>{const sortedEvents=useMemo(()=>[...events].sort((a,b)=>new Date(a.date).getTime()-new Date(b.date).getTime()),[events]);const getTypeColor=(type:string)=>{switch(type){case'birth':return'#32FF00';case'marriage':return'#00D9FF';case'death':return'#FF0000';default:return'#FFD700';}};return<div className="timeline-viewer"><h2>Timeline</h2><div className="timeline-container"><div className="timeline-line"/>{sortedEvents.map((event,idx)=>(<div key={idx}className="timeline-item"><div className="timeline-dot"style={{backgroundColor:getTypeColor(event.type)}}/><div className="timeline-content"><h4>{event.title}</h4><p className="timeline-date">{new Date(event.date).toLocaleDateString('it-IT')}</p>{event.description&&<p className="timeline-desc">{event.description}</p>}</div></div>))}</div></div>;};export default TimelineViewer;
